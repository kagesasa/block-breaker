# 修正版データについて

この `playable_game` フォルダの中身一式を、GitHub Pagesの公開フォルダに上書きアップロードしてください。

## 修正点
- GitHub Pages用のパス設定 (`/block-breaker/`) を適用済みです。
- エラー時に真っ白にならず、エラーメッセージが表示されるようにしました。

## 手順
1. GitHubのリポジトリを開く
2. `Add file` -> `Upload files` を選択
3. この `playable_game` フォルダの中にあるファイル全て（index.html, assetsフォルダなど）をドラッグ＆ドロップ
4. `Commit changes` を押して保存
5. 数分待ってからページをリロード（キャッシュが残っている場合はスーパーリロード: Cmd+Shift+R）
